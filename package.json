{
  "name": "deputies-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "convert": "node scripts/convert-csv-to-json.js",
    "fetch": "node scripts/fetch-data.js",
    "refresh": "npm run fetch && npm run convert && npm run build"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
